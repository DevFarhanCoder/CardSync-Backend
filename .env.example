# --- Server ---
PORT=8080
NODE_ENV=development
CORS_ORIGIN=http://localhost:5173,http://localhost:3000

# --- MongoDB ---
MONGODB_URI=mongodb+srv://<user>:<pass>@cluster0.mongodb.net/cardsync?retryWrites=true&w=majority

# --- Auth (JWT) ---
JWT_SECRET=replace-with-a-strong-secret
JWT_EXPIRES_IN=7d

# --- Supabase (Optional analytics forwarder) ---
SUPABASE_URL=
SUPABASE_SERVICE_ROLE_KEY=
SUPABASE_SCHEMA=public
